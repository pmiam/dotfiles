#!/usr/bin/env sh

PORT=8022
REPONAME={user}@{hostname}

# repos located on home NAS under user /home/borg/
# keys located on local machine under XDG_CONFIG_DIR/borg
borg init \
     --encryption keyfile \
     --make-parent-dirs \
     ssh://borg@nas.home.lan:$PORT/~/backups/$REPONAME
